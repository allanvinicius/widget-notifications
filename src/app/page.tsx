import { Widget } from "@/components/Widget";

export default function Home() {
  return (
    <main className="h-screen bg-zinc-50 dark:bg-zinc-950 flex items-center justify-center text-zinc-800 dark:text-zinc-50">
      <Widget />
    </main>
  );
}
